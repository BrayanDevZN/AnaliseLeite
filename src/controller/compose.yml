services:
    redis:
      image: redis:alpine
      ports:
        - "6379:6379"

    pipeline:
      build:
        context: ../..
        dockerfile: src/controller/Dockerfile

      depends_on:
        - redis
    